import { AddContact } from "@/components/AddContact";

export default function Home() {
  return (
    <div className="flex justify-center items bg-white w-screen h-screen">
      <AddContact />
    </div>
  );
}
